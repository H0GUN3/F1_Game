**# AGENTS.md - GitHub 프로젝트 관리 가이드라인

## 목차
1. [브랜치 전략](#브랜치-전략)
2. [커밋 컨벤션](#커밋-컨벤션)
3. [Pull Request 규칙](#pull-request-규칙)
4. [이슈 관리](#이슈-관리)
5. [코드 리뷰](#코드-리뷰)
6. [배포 프로세스](#배포-프로세스)
7. [긴급 상황 대응](#긴급-상황-대응)

---

## 브랜치 전략

### 브랜치 구조
```
main (production)
  ↑
develop (development)
  ↑
feature/* (기능 개발)
bugfix/* (버그 수정)
hotfix/* (긴급 수정)
release/* (배포 준비)
```

### 브랜치 명명 규칙

#### Feature 브랜치
```bash
feature/기능명-간단한설명
```
**예시:**
- `feature/player-movement`
- `feature/collision-detection`
- `feature/ui-main-menu`

#### Bugfix 브랜치
```bash
bugfix/이슈번호-버그설명
```
**예시:**
- `bugfix/#123-car-physics-bug`
- `bugfix/rendering-lag`

#### Hotfix 브랜치
```bash
hotfix/심각도-문제설명
```
**예시:**
- `hotfix/critical-crash-on-start`
- `hotfix/security-token-leak`

#### Release 브랜치
```bash
release/v버전번호
```
**예시:**
- `release/v1.0.0`
- `release/v1.2.3`

### 브랜치 생성 및 전환

```bash
# develop에서 최신 코드 받기
git checkout develop
git pull origin develop

# 새 feature 브랜치 생성
git checkout -b feature/track-editor

# 작업 후 푸시
git add .
git commit -m "feat: 트랙 에디터 기본 구조 구현"
git push origin feature/track-editor
```

### 브랜치 삭제 규칙

```bash
# 로컬 브랜치 삭제 (merge 후)
git branch -d feature/completed-feature

# 원격 브랜치 삭제
git push origin --delete feature/completed-feature
```

**삭제 시점:**
- PR이 merge된 후 즉시 삭제
- 30일 이상 활동 없는 브랜치는 자동 삭제 검토

---

## 커밋 컨벤션

### 커밋 메시지 형식

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 종류

| Type | 설명 | 예시 |
|------|------|------|
| `feat` | 새로운 기능 추가 | `feat: 레이싱 트랙 충돌 감지 추가` |
| `fix` | 버그 수정 | `fix: 차량 물리 계산 오류 수정` |
| `docs` | 문서 수정 | `docs: README에 설치 가이드 추가` |
| `style` | 코드 포맷팅 (동작 변경 없음) | `style: 코드 들여쓰기 정리` |
| `refactor` | 코드 리팩토링 | `refactor: 렌더링 로직 최적화` |
| `test` | 테스트 코드 | `test: 충돌 감지 유닛 테스트 추가` |
| `chore` | 빌드/설정 변경 | `chore: Gradle 의존성 업데이트` |
| `perf` | 성능 개선 | `perf: 타일 렌더링 속도 개선` |
| `ci` | CI/CD 설정 | `ci: GitHub Actions 워크플로우 추가` |
| `build` | 빌드 시스템 | `build: LibGDX 버전 업그레이드` |
| `revert` | 커밋 되돌리기 | `revert: feat: 트랙 에디터 제거` |

### Scope (선택사항)

프로젝트 영역 지정:
- `core`: 핵심 게임 로직
- `ui`: 사용자 인터페이스
- `physics`: 물리 엔진
- `render`: 렌더링
- `audio`: 오디오 시스템
- `network`: 네트워크 (멀티플레이어)

### Subject 작성 규칙

1. **50자 이내**로 간결하게
2. **명령문** 사용 (동사 원형)
3. **마침표 없이**
4. **한글** 또는 **영어** 일관성 유지

**좋은 예:**
```
feat(core): 차량 회전 로직 구현
fix(physics): Box2D 충돌 버그 수정
docs: 프로젝트 설치 가이드 작성
```

**나쁜 예:**
```
fixed some bugs  (너무 모호함)
트랙 추가했음.  (마침표 사용)
Feature/차량시스템 완성  (Type 누락)
```

### Body (선택사항)

**작성 시점:**
- 변경 이유가 명확하지 않을 때
- 복잡한 로직 설명이 필요할 때
- 여러 파일 수정이 포함될 때

**형식:**
```
feat(physics): 차량 드리프트 시스템 추가

- 속도와 회전각에 따른 드리프트 계산 추가
- 타이어 마찰력 시뮬레이션 구현
- 드리프트 이펙트 파티클 통합

기존 물리 엔진과 호환성 유지하며 구현
```

### Footer (선택사항)

**이슈 참조:**
```
Closes #123
Resolves #456
Related to #789
```

**Breaking Changes:**
```
BREAKING CHANGE: 기존 PhysicsEngine API 제거
새로운 PhysicsEngine2 사용 필요
```

### 커밋 예시

```bash
# 기본 커밋
git commit -m "feat: 플레이어 입력 시스템 추가"

# Scope 포함
git commit -m "fix(ui): 메인 메뉴 버튼 클릭 오류 수정"

# Body 포함
git commit -m "refactor(render): 타일 렌더링 최적화

- 뷰포트 외부 타일 렌더링 스킵
- 배치 렌더링으로 draw call 감소
- FPS 40 → 60으로 개선"

# 이슈 참조
git commit -m "fix: 트랙 경계 충돌 버그 수정

Closes #42"
```

### 커밋 수정/취소

```bash
# 마지막 커밋 메시지 수정
git commit --amend -m "새로운 메시지"

# 마지막 커밋에 파일 추가
git add forgotten-file.java
git commit --amend --no-edit

# 커밋 되돌리기 (히스토리 유지)
git revert <commit-hash>

# 커밋 취소 (로컬만)
git reset --soft HEAD~1  # 변경사항 유지
git reset --hard HEAD~1  # 변경사항 삭제
```

---

## Pull Request 규칙

### PR 생성 전 체크리스트

- [ ] 최신 develop 브랜치와 충돌 없음
- [ ] 로컬 테스트 완료
- [ ] 불필요한 파일 제외 (`.class`, `.log` 등)
- [ ] 커밋 메시지가 규칙에 맞음
- [ ] 코드 스타일 일관성 확인

### PR 제목 형식

```
[Type] 간단한 설명 (#이슈번호)
```

**예시:**
- `[Feature] 트랙 에디터 기본 기능 구현 (#15)`
- `[Bugfix] 차량 충돌 감지 오류 수정 (#42)`
- `[Refactor] 렌더링 시스템 최적화`

### PR 템플릿

```markdown
## 변경 사항
<!-- 무엇을 변경했는지 간략히 설명 -->

## 변경 이유
<!-- 왜 이 변경이 필요한지 설명 -->

## 주요 수정 파일
- `src/core/Player.java`
- `src/physics/CollisionDetector.java`

## 테스트 방법
1. 게임 실행
2. 트랙 선택
3. 충돌 시나리오 테스트

## 스크린샷/영상
<!-- 있다면 첨부 -->

## 관련 이슈
Closes #42
Related to #15

## 체크리스트
- [x] 로컬 테스트 완료
- [x] 코드 스타일 준수
- [ ] 문서 업데이트 필요
- [x] Breaking Changes 없음
```

### PR 리뷰 프로세스

```
1. PR 생성
   ↓
2. 자동 테스트 실행 (CI)
   ↓
3. 코드 리뷰 요청
   ↓
4. 리뷰어 피드백
   ↓
5. 수정 및 재푸시
   ↓
6. 승인 (Approve)
   ↓
7. Merge
```

### Merge 규칙

**Merge 방식:**
- **Squash and Merge**: Feature 브랜치 (기본)
- **Merge Commit**: Release 브랜치
- **Rebase and Merge**: Hotfix 브랜치

**Merge 조건:**
- 최소 1명 이상 승인 (Approve)
- CI 테스트 통과
- 충돌(Conflict) 해결 완료

```bash
# develop 브랜치로 merge
git checkout develop
git merge --no-ff feature/new-feature
git push origin develop
```

### PR 라벨 시스템

| 라벨 | 용도 | 색상 |
|------|------|------|
| `feature` | 새 기능 | 🟦 파랑 |
| `bugfix` | 버그 수정 | 🟥 빨강 |
| `documentation` | 문서 작업 | 📘 회색 |
| `enhancement` | 기능 개선 | 🟩 초록 |
| `urgent` | 긴급 처리 | 🟧 주황 |
| `needs-review` | 리뷰 필요 | 🟨 노랑 |
| `work-in-progress` | 작업 중 | 🟪 보라 |
| `ready-to-merge` | 병합 가능 | ✅ 초록 |

---

## 이슈 관리

### 이슈 템플릿

#### Bug Report
```markdown
## 버그 설명
<!-- 버그를 명확하게 설명 -->

## 재현 방법
1. 게임 시작
2. 트랙 선택
3. 충돌 발생

## 예상 동작
<!-- 정상적으로 어떻게 동작해야 하는지 -->

## 실제 동작
<!-- 실제로 어떻게 동작하는지 -->

## 환경
- OS: Windows 11
- Java 버전: 17
- LibGDX 버전: 1.12.0

## 스크린샷/로그
<!-- 있다면 첨부 -->
```

#### Feature Request
```markdown
## 기능 설명
<!-- 추가하고 싶은 기능 설명 -->

## 필요 이유
<!-- 왜 이 기능이 필요한지 -->

## 제안하는 구현 방법
<!-- 아이디어가 있다면 -->

## 대안
<!-- 다른 방법이 있다면 -->

## 우선순위
- [ ] 긴급 (Critical)
- [x] 높음 (High)
- [ ] 보통 (Medium)
- [ ] 낮음 (Low)
```

### 이슈 라벨

| 라벨 | 설명 |
|------|------|
| `bug` | 버그 |
| `feature` | 새 기능 요청 |
| `enhancement` | 기존 기능 개선 |
| `question` | 질문 |
| `help wanted` | 도움 필요 |
| `good first issue` | 초보자 적합 |
| `duplicate` | 중복 이슈 |
| `invalid` | 유효하지 않음 |
| `wontfix` | 수정 안함 |

### 이슈 관리 워크플로우

```
1. 이슈 생성
   ↓
2. 라벨 지정
   ↓
3. 담당자 할당 (Assignee)
   ↓
4. 마일스톤 설정 (선택)
   ↓
5. 작업 시작 (브랜치 생성)
   ↓
6. PR 생성 (이슈 참조)
   ↓
7. Merge 후 이슈 자동 Close
```

---

## 코드 리뷰

### 리뷰어 체크리스트

#### 기능성
- [ ] 요구사항을 충족하는가?
- [ ] 예상대로 동작하는가?
- [ ] 엣지 케이스가 처리되었는가?

#### 코드 품질
- [ ] 코드가 읽기 쉬운가?
- [ ] 중복 코드가 없는가?
- [ ] 적절한 네이밍을 사용했는가?

#### 성능
- [ ] 불필요한 연산이 없는가?
- [ ] 메모리 누수 가능성은 없는가?
- [ ] 최적화가 필요한 부분은 없는가?

#### 보안
- [ ] 입력 검증이 되어있는가?
- [ ] 민감 정보가 노출되지 않는가?

#### 테스트
- [ ] 테스트 코드가 충분한가?
- [ ] 모든 테스트가 통과하는가?

### 리뷰 코멘트 작성 가이드

**좋은 코멘트:**
```
💡 제안: 이 메서드를 별도 클래스로 분리하면 
재사용성이 높아질 것 같습니다.

이유: 현재 Player 클래스가 너무 많은 책임을 가지고 있어서
유지보수가 어려울 수 있습니다.
```

**나쁜 코멘트:**
```
이거 이상한데요?
고쳐주세요.
```

### 리뷰 상태

- **Comment**: 의견 제시 (승인 불필요)
- **Approve**: 승인 (merge 가능)
- **Request Changes**: 수정 요청 (merge 불가)

---

## 배포 프로세스

### 버전 관리 (Semantic Versioning)

```
v메이저.마이너.패치

예: v1.2.3
```

- **메이저 (Major)**: 호환성 없는 API 변경
- **마이너 (Minor)**: 하위 호환 기능 추가
- **패치 (Patch)**: 하위 호환 버그 수정

### Release 브랜치 생성

```bash
# develop에서 release 브랜치 생성
git checkout develop
git pull origin develop
git checkout -b release/v1.2.0

# 버전 업데이트 (build.gradle 등)
# 버전 번호 변경, 문서 업데이트

git add .
git commit -m "chore: v1.2.0 release 준비"
git push origin release/v1.2.0
```

### Release PR 템플릿

```markdown
## Release v1.2.0

### 주요 변경사항
- 트랙 에디터 기능 추가
- 차량 물리 엔진 개선
- UI 반응성 향상

### 버그 수정
- 충돌 감지 오류 수정 (#42)
- 렌더링 지연 개선 (#53)

### Breaking Changes
- 없음

### 업그레이드 가이드
<!-- 필요시 작성 -->

### 체크리스트
- [x] 모든 테스트 통과
- [x] 문서 업데이트
- [x] CHANGELOG 업데이트
- [x] 버전 번호 변경
```

### Release 후 작업

```bash
# main에 merge
git checkout main
git merge release/v1.2.0
git tag v1.2.0
git push origin main --tags

# develop에도 merge (버전 번호 동기화)
git checkout develop
git merge release/v1.2.0
git push origin develop

# release 브랜치 삭제
git branch -d release/v1.2.0
git push origin --delete release/v1.2.0
```

### GitHub Release 생성

1. GitHub 저장소 → Releases
2. **Draft a new release**
3. Tag: `v1.2.0` 선택/생성
4. Release title: `v1.2.0 - 트랙 에디터 추가`
5. 변경사항 작성 (CHANGELOG 복사)
6. 빌드 파일 첨부 (선택)
7. **Publish release**

---

## 긴급 상황 대응

### Hotfix 프로세스

**시나리오:** 프로덕션에서 치명적 버그 발견

```bash
# main에서 hotfix 브랜치 생성
git checkout main
git pull origin main
git checkout -b hotfix/critical-crash

# 버그 수정
# 테스트

git add .
git commit -m "fix: 치명적 충돌 버그 긴급 수정"
git push origin hotfix/critical-crash
```

### Hotfix PR 및 Merge

```markdown
## 🚨 긴급 Hotfix: 게임 시작 시 크래시

### 버그 설명
게임 시작 시 NullPointerException 발생

### 원인
TiledMap 로딩 전 렌더링 시도

### 수정 내용
- MapRenderer에 null 체크 추가
- 초기화 순서 변경

### 영향 범위
- 모든 사용자
- v1.2.0 이상 버전

### 테스트 완료
- [x] 로컬 테스트
- [x] 다양한 맵 테스트
```

**Merge 순서:**
```bash
# main에 먼저 merge
git checkout main
git merge hotfix/critical-crash
git tag v1.2.1
git push origin main --tags

# develop에도 merge
git checkout develop
git merge hotfix/critical-crash
git push origin develop

# hotfix 브랜치 삭제
git branch -d hotfix/critical-crash
git push origin --delete hotfix/critical-crash
```

### 롤백 프로세스

```bash
# 특정 커밋으로 되돌리기
git revert <bad-commit-hash>
git push origin main

# 태그 제거 (필요시)
git tag -d v1.2.1
git push origin :refs/tags/v1.2.1

# 새 hotfix 버전 배포
git tag v1.2.2
git push origin main --tags
```

---

## Git 명령어 치트시트

### 기본 명령어

```bash
# 저장소 복제
git clone <repository-url>

# 상태 확인
git status

# 변경사항 추가
git add .
git add <file>

# 커밋
git commit -m "message"

# 푸시
git push origin <branch>

# 풀
git pull origin <branch>

# 브랜치 목록
git branch
git branch -a  # 원격 포함
```

### 브랜치 관리

```bash
# 브랜치 생성
git branch <branch-name>

# 브랜치 전환
git checkout <branch-name>

# 브랜치 생성 + 전환
git checkout -b <branch-name>

# 브랜치 삭제
git branch -d <branch-name>

# 원격 브랜치 삭제
git push origin --delete <branch-name>
```

### 병합 및 충돌 해결

```bash
# 병합
git merge <branch-name>

# 충돌 발생 시
# 1. 충돌 파일 수정
# 2. git add <file>
# 3. git commit

# 병합 취소
git merge --abort
```

### 히스토리 관리

```bash
# 로그 확인
git log
git log --oneline
git log --graph --oneline --all

# 특정 커밋으로 되돌리기
git revert <commit-hash>

# 커밋 취소 (주의!)
git reset --soft HEAD~1  # 변경사항 유지
git reset --hard HEAD~1  # 변경사항 삭제
```

### Stash (임시 저장)

```bash
# 작업 임시 저장
git stash

# 저장 목록 확인
git stash list

# 저장된 작업 적용
git stash apply
git stash pop  # 적용 + 삭제

# 저장된 작업 삭제
git stash drop
```

---

## 팀 협업 규칙

### 작업 시작 전

1. **항상 최신 코드 받기**
```bash
git checkout develop
git pull origin develop
```

2. **새 브랜치 생성**
```bash
git checkout -b feature/my-feature
```

### 작업 중

1. **자주 커밋** (작은 단위로)
2. **의미있는 커밋 메시지**
3. **주기적으로 develop와 동기화**
```bash
git checkout develop
git pull origin develop
git checkout feature/my-feature
git merge develop
```

### 작업 완료 후

1. **셀프 코드 리뷰**
2. **불필요한 파일 제외**
3. **PR 생성 및 리뷰 요청**
4. **피드백 반영**

### 충돌 발생 시

```bash
# 충돌 확인
git status

# 충돌 파일 열어서 수정
# <<<<<<< HEAD
# 현재 브랜치 코드
# =======
# 병합하려는 브랜치 코드
# >>>>>>> branch-name

# 수정 후
git add <file>
git commit -m "fix: merge conflict 해결"
```

---

## .gitignore 설정

```gitignore
# LibGDX 프로젝트
*.class
*.log
*.jar
*.war
*.ear
bin/
build/
out/
.gradle/
gradle-app.setting

# IDE
.idea/
*.iml
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Android
local.properties
*.apk
*.ap_
*.dex

# 게임 관련
saves/
logs/
temp/
cache/

# Tiled 임시 파일
*.tmx~
*.tsx~**
